version: '3'
services:
  web:
    container_name: backend_assignment
    build:
          context: ../
          dockerfile: docker/Dockerfile
    volumes:
      - ../:/app
    command: python manage.py runserver 0.0.0.0:8000
    ports:
        - 6600:8000